---
import Base from '../layouts/Base.astro';
import AnimatedTerminal from '../components/AnimatedTerminal.astro';
import FeatureCard from '../components/FeatureCard.astro';
---

<Base title="Sixarms - AI-Powered Dev Progress Tracker">
  <!-- Hero Section -->
  <section class="hero">
    <!-- Background Grid -->
    <div class="hero-grid"></div>

    <div class="container">
      <div class="hero-content">
        <div class="hero-badge animate-fade-up">
          <span class="badge-dot"></span>
          Open Source • MIT License
        </div>

        <h1 class="hero-title animate-fade-up delay-1">
          Stop writing daily logs <span class="highlight">manually.</span>
        </h1>
        <p class="hero-subtitle animate-fade-up delay-2">
          Your commits tell a story. Let AI write it.
        </p>

        <AnimatedTerminal title="sixarms-terminal" class="hero-terminal animate-fade-scale delay-3">
          <div class="terminal-animation">
            <div class="line line-1">
              <span class="prompt">$</span> <span class="command">sixarms --init</span>
            </div>
            <div class="line line-2">
              <span class="success">✓</span> <span class="output">Scanning git repositories...</span>
            </div>
            <div class="line line-3">
              <span class="success">✓</span> <span class="output">Auto-classifying today's commits...</span>
            </div>
            <div class="line line-4">
              <span class="success">✓</span> <span class="output">Generating daily summary with AI...</span>
            </div>
            <div class="line line-5">
              <span class="prompt">></span> <span class="highlight-text">Ready. Your dev progress is now tracked.</span>
            </div>
            <div class="line line-6">
              <span class="cursor"></span>
            </div>
          </div>
        </AnimatedTerminal>

        <div class="hero-cta animate-fade-up delay-4">
          <a href="/download" class="btn btn-primary glow-pulse">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="7 10 12 15 17 10"></polyline>
              <line x1="12" y1="15" x2="12" y2="3"></line>
            </svg>
            Download for macOS
          </a>
          <a href="https://github.com/888wing/sixarms" target="_blank" rel="noopener" class="btn btn-secondary">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            View on GitHub
          </a>
        </div>

        <!-- Stats -->
        <div class="hero-stats animate-fade-up delay-5">
          <div class="stat">
            <span class="stat-value">100%</span>
            <span class="stat-label">Local Storage</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat">
            <span class="stat-value">MIT</span>
            <span class="stat-label">Open Source</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat">
            <span class="stat-value">macOS</span>
            <span class="stat-label">Native App</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features" id="features">
    <div class="container">
      <p class="section-label">// FEATURES</p>
      <h2 class="section-title">What Sixarms Does</h2>

      <div class="features-grid">
        <FeatureCard
          title="Auto Tracking"
          description="Automatically detects Git changes across all your projects. No manual logging required."
          terminalContent={[
            "$ git log --oneline",
            "",
            "> Commits detected: 12",
            "> Files changed: 28",
            "> Auto-logged to Sixarms ✓"
          ]}
        />

        <FeatureCard
          title="Smart Classify"
          description="AI categorizes your work: feature development, bug fixes, refactoring, and more."
          terminalContent={[
            "$ sixarms classify",
            "",
            "> feature: 45%",
            "> bugfix: 30%",
            "> refactor: 25%"
          ]}
        />

        <FeatureCard
          title="Daily Summary"
          description="Generates meaningful daily summaries of your development progress automatically."
          terminalContent={[
            "$ sixarms summary --today",
            "",
            "> \"Implemented user auth",
            "   with JWT tokens and",
            "   added password reset...\""
          ]}
        />
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="how-it-works">
    <div class="container">
      <p class="section-label">// HOW IT WORKS</p>
      <h2 class="section-title">Simple Setup, Powerful Tracking</h2>

      <div class="steps">
        <div class="step">
          <div class="step-number">01</div>
          <h3>Add Your Projects</h3>
          <p>Point Sixarms to your Git repositories. Supports any folder structure.</p>
        </div>

        <div class="step">
          <div class="step-number">02</div>
          <h3>Set Your Schedule</h3>
          <p>Configure auto-scan intervals. Daily, hourly, or on-demand.</p>
        </div>

        <div class="step">
          <div class="step-number">03</div>
          <h3>Let AI Do The Work</h3>
          <p>Grok AI analyzes your commits and generates intelligent summaries.</p>
        </div>
      </div>

      <div class="cta-section">
        <a href="/download" class="btn btn-primary">
          Get Started Free
        </a>
      </div>
    </div>
  </section>
</Base>

<style>
  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 8rem 0 4rem;
    position: relative;
    overflow: hidden;
  }

  /* Background Grid */
  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(0, 255, 65, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 255, 65, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  /* Hero Badge */
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: rgba(0, 255, 65, 0.1);
    border: 1px solid rgba(0, 255, 65, 0.2);
    border-radius: 100px;
    font-size: 0.8rem;
    color: var(--accent-green);
    margin-bottom: 1.5rem;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    background: var(--accent-green);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  .hero-title {
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 800;
    line-height: 1.05;
    margin-bottom: 1.5rem;
    color: var(--text-primary);
    font-family: var(--font-display);
    letter-spacing: -0.04em;
  }

  .hero-title .highlight {
    display: inline-block;
    color: var(--accent-green);
    text-shadow: 0 0 40px rgba(0, 255, 65, 0.6);
    position: relative;
  }

  .hero-title .highlight::after {
    content: '';
    position: absolute;
    bottom: 0.05em;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--accent-green), var(--accent-cyan));
    opacity: 0.6;
    border-radius: 2px;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.35rem);
    font-weight: 500;
    color: var(--text-muted);
    margin-bottom: 3rem;
    font-family: var(--font-heading);
    letter-spacing: -0.01em;
  }

  .hero-terminal {
    margin-bottom: 2.5rem;
    text-align: left;
  }

  /* Hero Stats */
  .hero-stats {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2rem;
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border-primary);
  }

  .stat {
    text-align: center;
  }

  .stat-value {
    display: block;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-primary);
    font-family: var(--font-display);
  }

  .stat-label {
    display: block;
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-top: 0.25rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .stat-divider {
    width: 1px;
    height: 30px;
    background: var(--border-primary);
  }

  .terminal-animation .line {
    margin-bottom: 0.5rem;
    opacity: 0;
    animation: fadeIn 0.5s ease forwards;
  }

  .line-1 { animation-delay: 0.5s; }
  .line-2 { animation-delay: 1.5s; }
  .line-3 { animation-delay: 2.5s; }
  .line-4 { animation-delay: 3.5s; }
  .line-5 { animation-delay: 4.5s; }
  .line-6 { animation-delay: 5s; }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(5px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .prompt { color: var(--accent-cyan); }
  .command { color: var(--text-primary); }
  .success { color: var(--accent-green); }
  .output { color: var(--text-secondary); }
  .highlight-text { color: var(--accent-green); }

  .cursor {
    display: inline-block;
    width: 10px;
    height: 1.2em;
    background: var(--text-primary);
    animation: blink 1s step-end infinite;
    vertical-align: text-bottom;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .hero-cta {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Features Section */
  .features {
    padding: 6rem 0;
    background: linear-gradient(180deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
  }

  .section-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--accent-cyan);
    margin-bottom: 0.5rem;
    text-align: center;
  }

  .section-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-family: var(--font-display);
    font-weight: 700;
    text-align: center;
    color: var(--text-primary);
    margin-bottom: 3rem;
    letter-spacing: -0.02em;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  /* How It Works Section */
  .how-it-works {
    padding: 6rem 0;
  }

  .steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .step {
    text-align: center;
    padding: 2rem;
  }

  .step-number {
    font-size: 2.5rem;
    font-weight: 800;
    font-family: var(--font-display);
    color: var(--accent-green);
    opacity: 0.3;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }

  .step h3 {
    font-size: 1.1rem;
    font-family: var(--font-heading);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    letter-spacing: -0.01em;
  }

  .step p {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .cta-section {
    text-align: center;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero {
      padding: 6rem 0 3rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }

    .btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .hero-stats {
      flex-wrap: wrap;
      gap: 1.5rem;
    }

    .stat-divider {
      display: none;
    }

    .hero-badge {
      font-size: 0.7rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 1.75rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
